# Security
# REQUIRED: Set this to a secure random string (e.g., generated via `openssl rand -hex 32`)
APP_API_KEY=change-me-to-a-secure-random-key

# Database
DATABASE_URL=postgresql://user:password@localhost:5432/kasparro

# Ingestion
INJECT_FAILURE=false
